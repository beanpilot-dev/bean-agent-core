# Quickstart
```
# Setup environment
# If you don't have `uv`, I strongly recommend you use it.
# Try it by `pip install uv`
uv sync

# Start the service
uv run python -m agent_core.main --port 8000
```
