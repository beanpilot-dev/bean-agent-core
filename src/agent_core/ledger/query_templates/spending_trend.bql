-- id: spending_trend
-- desc: Monthly trend of total spending (or income) for an account pattern.
--       Use this to identify rising/falling patterns over multiple months.
--       Income accounts return negative sums (beancount convention).
-- params:
--   account_pattern  regex matched against account path  e.g. ^Expenses:Food
--   start            period start date inclusive         e.g. 2025-07-01
--   end              period end date exclusive           e.g. 2026-02-01
SELECT year, month, sum(position) AS amount
WHERE account ~ '{account_pattern}'
  AND date >= {start} AND date < {end}
GROUP BY year, month ORDER BY year, month
